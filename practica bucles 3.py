for i in range(5,50,3): # (rango donde empieza, rango donde termina, de a cuantos numeros va saltando)
	print(f"valor de la variable {i}") # la funcion f sirve para concatenar el contenido de la variable en un string IMPORTANTE poner la variable entre llaves {}
